<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Site Under Maintenance</title>
  <style>
    /* Reset styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Background gradient */
    body {
      background: linear-gradient(to bottom, #1d1e22, #30353a);
    }

    /* Main container */
    .container {
      width: 80%;
      max-width: 800px;
      margin: 0 auto;
      padding: 50px;
      background: #fff;
      border-radius: 5px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.3);
      margin-top: 50px;
      margin-bottom: 50px;
    }

    /* Header */
    h1 {
      text-align: center;
      color: #fff;
      font-size: 3rem;
      margin-bottom: 30px;
      text-shadow: 1px 1px #000;
    }

    /* Subheading */
    h2 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 30px;
      color: #fff;
      text-shadow: 1px 1px #000;
    }

    /* Form */
    form {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 50px;
    }

    /* Form fields */
    label, input {
      display: block;
      margin-bottom: 20px;
    }

    /* Form submit button */
    input[type="submit"] {
      padding: 10px;
      background: #4c8bf5;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    input[type="submit"]:hover {
      background: #3466c2;
    }

    /* Success message */
    .success {
      text-align: center;
      margin-top: 50px;
      font-size: 1.5rem;
      color: #fff;
      text-shadow: 1px 1px #000;
    }

    /* Error message */
    .error {
      text-align: center;
      margin-top: 50px;
      font-size: 1.5rem;
      color: #fff;
      text-shadow: 1px 1px #000;
    }

    /* Colors */
    .color1 { background-color: #f7c5c0; }
    .color2 { background-color: #f3a0a3; }
    .color3 { background-color: #e37b79; }
    .color4 { background-color: #d75754; }
    .color5 { background-color: #c93431; }
    .color6 { background-color: #f5e7b5; }
    .color7 { background-color: #f0d38c; }
    .color8 { background-color: #ebb964; }
    .color9 { background-color: #e5a93b; }
    .color10 { background-color: #e08e12; }